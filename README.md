# proximity-sensor
STM32 IoT monitor using the VCNL4200 proximity &amp; ambient light sensor. Detects events via hardware interrupt and publishes real-time MQTT alerts over Wi-Fi (ESP-AT). Built with STM32 HAL and HiveMQ public broker.
